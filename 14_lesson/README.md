### SELECT * FROM system.replicas ...

Результат выполнения запроса можно найти в select.json


### SELECT getMacro('replica'), * FROM remote(...) ...

Результат выполнения запроса можно найти в select_remote.json


### Итоговый результат запроса show table nyc_taxi.trips_small:

Результат можно найти в show_table.txt или на изображении ниже:

![alt text](image.png)

